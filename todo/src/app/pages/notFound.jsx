import React from 'react';
import Main from '../components/main';
import PostAvatar from "../components/postAvatar";
import { Router, Route, Link } from 'react-router';
const NF =React.createClass({
  render(){
    const standardActions = ("");
    return(
      <div>
        NOT FOUND!
      </div>

    )}
  });

  export default NF;
